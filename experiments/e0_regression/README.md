# E0 — Zero-error regression

Validates the imperfect-perception path at identity lane/cue channels against
the frozen N=4 and N=16 semantic references. It owns regression metadata and
the signal/categorical wiring smokes. Generated runs go under
`benchmarks/results/e0_regression/`.
