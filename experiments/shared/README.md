# Shared experiment infrastructure

This directory is reserved for seed schedules, result schemas, and reusable
runner/analysis helpers. Experiment-specific behavior belongs in its own
experiment directory; the central dispatcher remains the compatibility entry
point while the migration is staged.
