# Shared plotting and metrics

Reusable result I/O belongs in `io/`, metric aggregation in `metrics/`, and
paper-facing figure generation in `figures/`. Experiment folders own only
analysis that is specific to their fixture or hypothesis.
