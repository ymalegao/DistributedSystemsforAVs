# ResilientDB bridge

This is the repository-owned bridge source that will be linked into the
materialized ResilientDB workspace at `.external/resilientdb/integration/omnet`.
The current vendored bridge remains in place until the generated patch and
fresh-clone build have been verified.
