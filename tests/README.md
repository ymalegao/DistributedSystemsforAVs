# Validation tests

The compatibility test modules remain in `fourway/` while imports are being
relocated. New unit, fixture, and golden tests belong under `tests/unit`,
`tests/fixtures`, and `tests/golden`; the final cleanup checkpoint runs both
the repository suite and experiment-local suites.
