# Four-way SUMO/OMNeT scenarios

The existing `fourway/` directory is the compatibility source while scenario
assets are grouped into `networks/`, `routes/`, `launch/`, `baselines/`, and
`rollback/`. Generated keys, overrides, binaries, and results belong in the
ignored paths described by the repository `.gitignore`.
